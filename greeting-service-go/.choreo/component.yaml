schemaVersion: 1.2

endpoints:
  # Step 1 - deploy with only this endpoint first.
  # Registers "create-students-quick-public" in APIM successfully.
  - name: create-students-quick-public
    displayName: Create Students Quick Public
    service:
      basePath: /
      port: 8080
    type: REST
    networkVisibilities:
      - Public
    schemaFilePath: openapi.yaml

  # # Step 2 - add this endpoint and redeploy.
  # # FAILURE: sanitizeQuery strips hyphens → tokens [create, students, public]
  # # All 3 tokens match "create-students-quick-public" in APIM Atlas search.
  # # Exact context path filter leaves 0 results → "cannot find exactly 1 API"
  # - name: create-students-public
  #   displayName: Create Students Public
  #   service:
  #     basePath: /
  #     port: 8080
  #   type: REST
  #   networkVisibilities:
  #     - Public
  #   schemaFilePath: openapi.yaml
